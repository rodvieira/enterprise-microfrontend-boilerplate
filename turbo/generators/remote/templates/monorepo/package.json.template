{
  "name": "{{packageName}}",
  "version": "0.0.0",
  "private": true,
  "description": "Generated by `pnpm turbo gen remote` — a placeholder remote scaffolded from the shared apps/dashboard/apps/admin pattern. Replace this description once real functionality is built.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "rspack serve",
    "build": "rspack build",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@enterprise-mfe/shared-types": "workspace:*",
    "@module-federation/enhanced": "{{moduleFederationVersion}}",
    "react": "{{reactVersion}}",
    "react-dom": "{{reactDomVersion}}",
    "react-router": "{{reactRouterVersion}}"
  },
  "devDependencies": {
    "@enterprise-mfe/config-typescript": "workspace:*",
    "@rspack/cli": "{{rspackCliVersion}}",
    "@rspack/core": "{{rspackCoreVersion}}",
    "@rspack/dev-server": "{{rspackDevServerVersion}}",
    "@tailwindcss/postcss": "{{tailwindPostcssVersion}}",
    "@testing-library/jest-dom": "{{testingLibraryJestDomVersion}}",
    "@testing-library/react": "{{testingLibraryReactVersion}}",
    "@testing-library/user-event": "{{testingLibraryUserEventVersion}}",
    "postcss": "{{postcssVersion}}",
    "postcss-loader": "{{postcssLoaderVersion}}",
    "tailwindcss": "{{tailwindcssVersion}}",
    "vitest": "{{vitestVersion}}"
  }
}
