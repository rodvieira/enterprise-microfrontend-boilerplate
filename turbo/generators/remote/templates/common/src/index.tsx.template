/**
 * Same async boundary every remote in this repository uses: Module
 * Federation must resolve shared singletons (react, react-dom, react-router)
 * before any application code runs.
 */
import('./bootstrap');
