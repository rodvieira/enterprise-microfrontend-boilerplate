interface PlaceholderPanelProps {
  label: string;
  onAnnounce: () => void;
}

/**
 * Generated by `pnpm turbo gen remote` — replace with this remote's real
 * feature. Its only job is to prove `src/internal/` is reachable from
 * `src/exposed/App.tsx` and nothing else, the same split every remote in
 * this repository uses.
 */
export function PlaceholderPanel({ label, onAnnounce }: PlaceholderPanelProps) {
  return (
    <div className="flex flex-col items-start gap-4 rounded-lg border border-(--color-border) p-6">
      <p className="text-sm text-(--color-text-muted)">
        {label} is scaffolded and composes inside the shell. Replace this panel with real content
        under `src/internal/`.
      </p>
      <button
        type="button"
        onClick={onAnnounce}
        className="rounded-(--radius-control) bg-(--color-brand-600) px-(--spacing-control-x) py-(--spacing-control-y) text-sm font-medium text-white hover:bg-(--color-brand-700)"
      >
        Publish a message on the host bus
      </button>
    </div>
  );
}
