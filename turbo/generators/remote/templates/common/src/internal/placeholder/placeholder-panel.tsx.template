interface PlaceholderPanelProps {
  label: string;
}

/**
 * Generated by `pnpm turbo gen remote` — replace with this remote's real
 * feature. Its only job is to prove `src/internal/` is reachable from
 * `src/exposed/App.tsx` and nothing else, the same split every remote in
 * this repository uses.
 */
export function PlaceholderPanel({ label }: PlaceholderPanelProps) {
  return (
    <div className="rounded-lg border border-(--color-border) p-6">
      <p className="text-sm text-(--color-text-muted)">
        {label} is scaffolded and composes inside the shell. Replace this
        panel with real content under `src/internal/`.
      </p>
    </div>
  );
}
