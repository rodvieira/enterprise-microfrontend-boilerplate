/// <reference types="@testing-library/jest-dom" />

// Registers the jest-dom matchers for the type-checker. Scoped to this
// package's tests so the shared React tsconfig stays free of testing
// dependencies.
